#include<stdio.h>

int main(){
          char r;
          printf("Enter character is ::");
          scanf("%c",&r);
          printf("ASCII value of character is %c = %d",r,r);          
          
          return 0;
}